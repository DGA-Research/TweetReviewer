# Secrets — never commit real credentials.
.env
.env.*
!.env.example
.streamlit/secrets.toml

# Python
__pycache__/
*.pyc
.venv/
venv/

# Local per-session working files (also tmpfs in Docker).
.sessions/
